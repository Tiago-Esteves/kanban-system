name: Keep Alive Render

on:
  schedule:
    - cron: "*/5 * * * *"  # roda a cada 5 minutos
  workflow_dispatch:        # permite rodar manualmente também

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl render app
        run: curl -s https://kanban-backend-cvn2.onrender.com > /dev/null
